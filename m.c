#include <stdio.h>

int size = 0; //Change this based on data


struct data{
   int size;
   char[size] data;
}

data[size] dataset;



int identifier(struct data size){
  int sum;
  int dataanalyzed;
  for(int i = 0; dataset[i].data = dataset[i].data + 'm'; i++){
    dataanalyzed++;
    for(int z = 0; dataset[i].data[z] = dataset[i].data[z]; z++){
        if(dataset[i].data[z] == size.data[z]){
            z++;
        }
      else{
         sum + z;
         break;
    }
}
  }
  return sum/datanalyzed;
}
